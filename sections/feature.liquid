<h2>{{section.settings.title}}</h2>
<p>{{section.settings.read}}</p>
<figure>{{section.settings.image1}}</figure>
{% schema %}
  {
    "name": "オリジナルセクション１",
    "tag": "section",
    "class": "original-section",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "見出し",
        "default": "テキストが入ります"
      },
      {
        "type": "textarea",
        "id": "read",
        "label": "リード文",
        "info": "改行時は文末に<br>を入れてください",
        "default": "リード文が入ります"
      },
      {
        "type": "image_picker",
        "id": "image1"
      }
    ],
    "presets": [
      {
      "name": "オリジナルセクション１"
      }
    ]
  }
{% endschema %}