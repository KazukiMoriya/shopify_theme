<h2>{{section.settings.title}}</h2>

{% schema %}
  {
    "name": "オリジナルセクション１",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "見出し",
        "default": "テキストが入ります"
      },
      {
        "type": "textarea",
        "id": "read",
        "label": "リード文",
        "default": "リード文が入ります"
      }
    ],
    "presets": [
      {
      "name": "オリジナルセクション１"
      }
    ]
  }
{% endschema %}