<link rel=”stylesheet” href=”https://unpkg.com/swiper/swiper-bundle.min.css”/>
<script src=”https://unpkg.com/swiper/swiper-bundle.min.js” defer></script>
<div class="swiper">
  <!-- Additional required wrapper -->
  <div class="swiper-wrapper">
    {% for block in section.blocks %}
    {% if block.settings.slide__anchor == true %}
    <div class="swiper-slide"><a href="{{block.settings.slide__url}}"><img src="{{ block.settings.slide__image | img_url : 'master' }}" alt="" width="" height="" loading="eager"></a></div>
    {% else %}
    <div class="swiper-slide"><img src="{{ block.settings.slide__image | img_url : 'master' }}" alt="" width="" height="" loading="eager"></div>
    {% endif %}
    {% endfor %}
  </div>
  <!-- If we need pagination -->
  <div class="swiper-pagination"></div>

  <!-- If we need navigation buttons -->
  <div class="swiper-button-prev"></div>
  <div class="swiper-button-next"></div>

  <!-- If we need scrollbar -->
  <div class="swiper-scrollbar"></div>
</div>
{% schema %}
  {
    "name": "メインビジュアル",
    "tag": "div",
    "class": "mainVisual",
    "blocks": [
      {
        "type": "slide",
        "name": "スライド",
        "limit": 3,
        "settings": [
          {
            "type": "image_picker",
            "id": "slide__image",
            "label": "スライドの画像"
          },
          {
            "type": "checkbox",
            "id": "slide__anchor",
            "label": "遷移先の有無",
            "info": "遷移するスライドの場合、チェックを入れてください。",
            "default": true
          },
          {
            "type": "text",
            "id": "slide__url",
            "label": "遷移先URL"
          }
        ]
      }
    ],
    "settings": [
      {
        "type": "text",
        "id": "slide__heading",
        "label": "見出し"
      }
    ],
    "presets": [
      {
        "name": "メインビジュアル"
      }
    ]
  }
{% endschema %}